# 02 — Build Plan

> Template. Replace every `<fill-in>`.

## Phases
1. Repo foundation — <fill-in>
2. Domain foundation — <fill-in>
3. Decision engine (controls before scoring) — <fill-in>
4. Frontend static experience — <fill-in>
5. API integration — <fill-in>
6. AI explanation (redaction + deterministic fallback) — <fill-in>
7. Execution simulator / state machine — <fill-in>
8. Fallback / point-of-no-return handling — <fill-in>
9. Polish (disclaimers, docs, deployment notes) — <fill-in>
10. Review and harden — <fill-in>

Each phase boundary triggers a fresh reviewer subagent (AGENTS.md §4).

## Commands (agents run exactly what's written here)
> Local verification must not require live external credentials or production infra.

| Purpose | Command |
|---------|---------|
| Build | `<fill-in>` |
| Unit tests | `<fill-in>` |
| Integration tests | `<fill-in>` |
| Lint / format | `<fill-in>` |
| Type-check | `<fill-in>` |
| Security / dependency scan | `<fill-in>` |
| UI smoke (render + interaction + screenshot) | `<fill-in>` |
